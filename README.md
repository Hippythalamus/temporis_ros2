# temporis_ros2
ROS2 integration package for Temporis — a framework for realistic network latency modeling in multi-agent systems.  Provides ROS2 nodes and launch files for injecting queue-based and transport-aware latency models into distributed robotics experiments.
